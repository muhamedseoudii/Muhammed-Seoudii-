import 'package:flutter/material.dart';

class AppliedPageView extends StatelessWidget {
  const AppliedPageView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
